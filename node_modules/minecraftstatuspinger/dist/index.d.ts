import { ServerStatusOptions, ServerStatus } from "./types.js";
/**
 * Looks up the server.
 * @example
 * Here's a simple example:
 * ```
 * let result = await mc.lookup({
 *    host: "mc.hypixel.net"
 *    port: 25565,
 *    timeout: 10000,
 *    ping: true,
 *    protocolVersion: 764
 *    throwOnParseError: false,
 *    disableSRV: false,
 *    disableJSONParse: false
 * })
 * ```
 */
declare function lookup(options?: ServerStatusOptions): Promise<ServerStatus>;
/**
 * Used for changing the default DNS servers.
 * @example
 * ```
    // Recommended servers
    mc.setDnsServers(["9.9.9.9", "1.1.1.1", "8.8.8.8"])
    // (Quad9, Cloudflare, Google)
    // Cloudflare is the fastest for DNS queries in most of the world.
```
 */
declare function setDnsServers(serverArray: Array<string>): Promise<boolean>;
declare const _default: {
    setDnsServers: typeof setDnsServers;
    lookup: typeof lookup;
};
export default _default;
//# sourceMappingURL=index.d.ts.map